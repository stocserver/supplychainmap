# Netlify configuration
[build]
  command = "npm run build"
  publish = ".next"

# Scheduled function for cache warming
[functions]
  directory = "netlify/functions"

# Schedule the cache-warmer function to run every 5 minutes
[functions."warm-cache"]
  schedule = "*/5 * * * *"
